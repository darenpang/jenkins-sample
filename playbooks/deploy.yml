---
- name: Deploy connectivity check
  hosts: "{{ target_group }}"
  gather_facts: false
  remote_user: "{{ login_user }}"
  tasks:
    - name: Ping target host
      ping:

    - name: Print deploy connectivity message
      debug:
        msg: "Deploy connectivity check succeeded on {{ inventory_hostname }} as {{ login_user }}"
